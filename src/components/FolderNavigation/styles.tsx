import { createStyles } from '@material-ui/core';
export type Styles = 'breadcrumb';

const styles = () =>
  createStyles({
    breadcrumb: {
      cursor: 'pointer',
    },
  });

export default styles;
